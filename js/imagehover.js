function show_new(element) {
  element.querySelector('.two').style.opacity = "1";
  element.querySelector('.default_image').style.opacity = "0";
}

function hide_new(element) {
  element.querySelector('.two').style.opacity = "0";
  element.querySelector('.default_image').style.opacity = "1";
}

